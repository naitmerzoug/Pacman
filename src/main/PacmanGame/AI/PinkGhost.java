package PacmanGame.AI;

import Moteurs.core.CoreEngine;
import PacmanGame.AI.Ghost;

public class PinkGhost extends Ghost {

    public PinkGhost(int id, double x, double y, CoreEngine coreEngine) {
        super(id, x, y, coreEngine);
    }
}
